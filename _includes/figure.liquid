<figure>
  {% if include.path %}
    <img src="{{ include.path }}" alt="{{ include.alt }}" {% if include.class %}class="{{ include.class }}"{% endif %}>
  {% endif %}
  {% if include.caption %}
    <figcaption>{{ include.caption }}</figcaption>
  {% endif %}
</figure>
